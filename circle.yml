dependencies:
    override:
        - bundle install
        - sudo pip install awscli
        
test:
    override:
        - echo "test"

deployment:
    aws:
    branch: master
        commands:
        - aws s3 sync . s3://$BUCKET/ --delete